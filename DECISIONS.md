# DECISIONS — журнал архитектурных решений

Формат: дата · решение · причина. Новые записи сверху.

---

## 2026-06-10 · Архитектура веб-интерфейса gateway-ui (Фаза 1, ПЛАН)
Новая веха: веб-UI для настройки шлюза без CLI. Утверждённые решения:
- **Стек — Go, один статичный бинарник** (этос «нативно, без Docker», как xray). Фронтенд встроен через embed.FS.
- **Поставка — готовый бинарник в релизе** (под x86/arm), на устройстве Go не нужен.
- **Доступ — только LAN + пароль**, bind на LAN-IP + iptables-ограничение. Редактирует секреты/рут — наружу нельзя. (TLS — позже.)
- **UI — тонкий оркестратор, не вторая реализация логики.** Источник истины не меняется: config.env, xray/domains/*.txt, systemd. Рендер config.json выносится из install.sh в общий `xray/render-config.sh`, чтобы install и UI звали один код.
- **Пользовательские домены из UI — вне репо**, в `/etc/gateway/domains/local.txt`; build-domains.sh читает и репо, и эту папку. Причина: deploy.sh делает rsync --delete и затёр бы правки пользователя.
- **Скоуп v1:** IP роутера, списки доменов, статус/управление. VPS/Reality-креды и zapret-стратегии — в config.env/CLI, UI расширим позже.
**Причина:** дать пользователю настраивать шлюз при установке и в работе без терминала.

## 2026-06-10 · Развёрнут каркас процесса (brownfield)
Проект уже существовал и работал. Применили методологию из `universal-dev-bootstrap.md`
в режиме brownfield: реверс-инжиниринг существующего кода в CANON/TASKS/DECISIONS/GOALS,
без проектирования с нуля. README/AGENT оставлены как пользовательская дока,
CANON — внутренняя правда об архитектуре.
**Причина:** навести порядок, чтобы состояние проекта жило в файлах, а не в контексте сессий.

## 2026-06-10 · Список доменов роутинга генерируется из xray/domains/*.txt (T6)
Было два разошедшихся источника: инлайн-список в config.template.json (315 proxy-доменов) и
осиротевшие xray/domains/*.txt (363 записи, код их не читал). Свели к одному: txt — источник истины,
[xray/build-domains.sh](xray/build-domains.sh) генерит JSON-массив, install.sh подставляет в `${ROUTING_DOMAINS}`.
**Причина:** пополнять домены по просьбам пользователей — дописать строку в маленький .txt, а не править гигантский JSON.
**Что сделано безопасно:** 23 proxy-домена, которых не было в txt, доположены в txt (без регрессии).
Steam/dota (11 доменов) оставлены в direct-правиле, в txt не перенесены.
**Изменение поведения:** 71 домен из txt, ранее не проксировавшийся, теперь идёт через VPS
(они курировались как «обходить» — совпадает с целью). Проверяется демонстрацией (GOALS G6/G7).
IP-правила Meta/Telegram остались захардкожены в шаблоне.

## 2026-06-10 · Подписи портов в install.sh приведены к реальности
`install.sh` при опросе помечал Vision как «основной», gRPC как «для Meta/Instagram» —
противоречило фактическому роутингу (всё через gRPC). Подписи исправлены.
**Причина:** проза (промпты установщика) разошлась с истиной (роутинг в config.template.json).

## (ранее, по коду/коммиту b549195) · gRPC :2083 — основной канал вместо Vision :443
Весь proxy-трафик идёт через VLESS Reality gRPC на нестандартном порту 2083.
Vision-outbound (`proxy`, :443) оставлен в конфиге, но в роутинге не используется.
**Причина:** РФ-провайдеры (ТСПУ/DPI) режут длинные TLS-сессии к зарубежному VPS на :443 —
Reality Vision там нестабилен (SYN не доходит, туннель висит). gRPC на :2083 проходит мимо фильтра.
**Откат:** в регионе без блокировки :443 можно вернуть домены на `proxy` (Vision) ради производительности.
Реф: [xray/config.template.json:635](xray/config.template.json:635).

## (ранее, по коду) · Telegram MTProto — через gRPC, не Vision
IP-подсети Telegram заведены на `proxy-mux` (gRPC).
**Причина:** `xtls-rprx-vision` рвёт не-TLS MTProto-трафик.
Реф: [xray/config.template.json:655](xray/config.template.json:655).
